rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read and write to the cashGames collection.
    // This is suitable for a public app where no user authentication is required.
    match /cashGames/{gameId} {
      allow read, write: if true;
    }
  }
}
